name: test_environment
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.7
  - pip
  - mamba
  - numpy
  - netCDF4
  - pysteps>=1.4,<2.0
  - dask
  - xarray
  # Test dependencies
  - pytest
